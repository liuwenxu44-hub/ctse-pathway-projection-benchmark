# Artificial values only. No study input, method, NNLS or MCMC execution.
source('R/ctse_benchmark.R');source('R/epic_scale_contract.R')
source('study/lib/endpoint_math.R')
source('study/lib/epic_diagnostics.R');source('study/lib/cts_from_draws.R')
tests<-list();check<-function(name,ok){stopifnot(isTRUE(ok));tests[[length(tests)+1L]]<<-data.frame(test=name,status='PASS')}
fails<-function(x)inherits(tryCatch(force(x),error=identity),'error')
good<-ctse_comparison_contract('CPM','CPM','conditional_expression','conditional_expression')
bad_unit<-ctse_comparison_contract('CPM','probability','conditional_expression','conditional_expression')
bad_target<-ctse_comparison_contract('CPM','CPM','mixture_contribution','conditional_expression')
bad_log<-ctse_comparison_contract('log2_CPM_plus_1','CPM','conditional_expression','conditional_expression')
check('missing_unit_contract_blocks',fails(ctse_vector_metrics(1:4,1:4)))
check('probability_truth_vs_CPM_blocks',fails(ctse_vector_metrics(1:4,1:4,comparison_contract=bad_unit)))
check('contribution_vs_conditional_blocks',fails(ctse_vector_metrics(1:4,1:4,comparison_contract=bad_target)))
check('log_vs_linear_amplitude_blocks',fails(ctse_vector_metrics(1:4,1:4,comparison_contract=bad_log)))
check('same_unit_amplitude_allowed',ctse_vector_metrics(1:4,1:4,comparison_contract=good)$rmse==0)
check('negative_linear_values_not_clipped',ctse_vector_metrics(-2:1,1:4,comparison_contract=good)$rmse==3)
check('nonfinite_not_silently_filtered',fails(ctse_vector_metrics(c(1,NA,3),1:3,comparison_contract=good)))
check('permuted_gene_keys_block',fails(ctse_vector_metrics(setNames(1:3,letters[1:3]),setNames(1:3,letters[3:1]),comparison_contract=good)))
check('constant_truth_rank_undefined',is.na(safe_rank(1:4,rep(2,4))))
check('failed_direction_is_not_zero',is.na(classify(NA_real_)))
check('boundary_point05_unchanged',identical(classify(c(-.051,-.05,0,.05,.051)),c('negative','zero','zero','zero','positive')))
g<-c('a','b','c');s<-c('s1','s2');B<-matrix(c(10,20,70,40,20,40),3,2,dimnames=list(g,s))
linear<-ctse_epic_linear_input(B,g,s);L<-ctse_epic_log_response(linear)
check('full_support_CPM_before_log',identical(L$values,log2(sweep(B,2,colSums(B),'/')*1e6+1)))
check('double_transform_blocks',fails(ctse_epic_log_response(L)))
sub<-ctse_epic_subset_response(L,c('a','b'))
check('subset_denominator_unchanged',identical(sub$full_denominator,colSums(B))&&identical(sub$values,L$values[1:2,,drop=FALSE]))
check('subset_renormalization_is_different',!identical(sub$values,log2(sweep(B[1:2,],2,colSums(B[1:2,]),'/')*1e6+1)))
check('duplicate_returned_gene_blocks',fails(ctse_epic_subset_response(L,c('a','a'))))
check('unknown_gene_is_not_zero_filled',fails(ctse_epic_subset_response(L,c('a','x'))))
check('input_ID_mismatch_blocks',fails(ctse_epic_linear_input(B,g,rev(s))))
check('practical_allows_declared_W',ctse_validate_fit_access(list(bulk=B,W=matrix(.5,2,2)),'practical'))
check('practical_oracle_access_blocks',fails(ctse_validate_fit_access(list(W=1,oracle_W=2),'practical')))
check('nested_proxy_access_blocks',fails(ctse_validate_fit_access(list(x=list(C2_proxy=1)),'practical')))
check('oracle_truth_access_still_blocks',fails(ctse_validate_fit_access(list(truth=1),'oracle')))
d<-class_summary(c('negative','zero','positive'),c('negative',NA,'positive'))
check('failure_accuracy_with_coverage',d['coverage']==2/3&&d['overall_accuracy']==1&&is.na(d['balanced_accuracy_macro_recall']))
# Direction is homogeneous of degree zero. A tiny contrast is not proof of a
# large expression effect, significance or a biological false-positive rate.
delta<-c(.3,-.1,.4);genes<-sprintf('sg%04d',1:3)
check('direction_does_not_measure_amplitude',abs(projection(delta,genes,'signal')['P']-projection(delta*1e-9,genes,'signal')['P'])<1e-12)
set.seed(90109);iid<-matrix(rnorm(4000*4),4000,4)
check('iid_diagnostics_pass',epic_diagnostic_matrix(iid)$status=='PASS')
check('constant_chains_do_not_pass',epic_diagnostic_matrix(matrix(1,100,4))$status=='CONSTANT_CHAIN')
shifted<-iid;shifted[,1]<-shifted[,1]+3
check('shifted_chains_do_not_pass',epic_diagnostic_matrix(shifted)$status!='PASS')
rec<-lapply(1:4,function(i){m<-matrix(iid[,i],ncol=1,dimnames=list(NULL,'artificial'));list(target_sha256='artificial',stage=1L,gene='g',schedule=c(nitt=5300,burnin=1300,thin=1),full_support_bounds=c(0,10),effective_seed=as.integer(i),rng_before_sampler=as.integer(c(1,i)),Sol=m,VCV=m,CTS=m,warnings=character())})
check('four_saved_chains_diagnosed',all(epic_diagnose_chains(rec)$status=='PASS'))
bad<-rec;bad[[2]]$effective_seed<-1L
check('reused_effective_seed_blocks',fails(epic_diagnose_chains(bad)))
bad<-rec;bad[[1]]$warnings<-'artificial warning'
check('warnings_cannot_claim_PASS',all(epic_diagnose_chains(bad)$status=='WARNING_REQUIRES_REVIEW'))
write.table(do.call(rbind,tests),stdout(),sep='\t',row.names=FALSE,quote=FALSE)
cat('SEMANTIC_REGRESSION_TESTS_PASS',length(tests),'MODEL_FITS_0\n')
